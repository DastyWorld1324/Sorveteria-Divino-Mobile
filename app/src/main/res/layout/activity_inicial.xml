<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".Inicial">

    <View
        android:id="@+id/containercima"
        style="@style/containers"
        android:layout_width="match_parent"
        android:layout_height="60dp"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent"


        />

    <EditText
        android:id="@+id/pesquisaproduto"
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintTop_toTopOf="@id/containercima"
        android:text="Oque você deseja?"
        android:fontFamily="@font/questrial"
        android:textSize="15dp"
        style="@style/pesquisa"
        />

    <ImageButton
        android:id="@+id/botaofiltros"
        android:layout_width="40dp"
        android:layout_height="40dp"
        app:layout_constraintLeft_toRightOf="@id/pesquisaproduto"
        app:layout_constraintTop_toTopOf="@id/containercima"
        app:layout_constraintBottom_toBottomOf="@id/containercima"
        android:padding="30dp"
        android:background="@drawable/formafiltro"
        android:layout_marginLeft="20dp"

        />


</androidx.constraintlayout.widget.ConstraintLayout>